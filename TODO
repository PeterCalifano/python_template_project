# Template Maintenance Backlog

- [ ] Add a project initialization helper that renames the distribution and import package placeholders in one step
- [ ] Add an example `pyproject.toml` section for console scripts
- [ ] Add optional pre-commit configuration once a preferred hook set is settled
- [ ] Add an example compiled extension build when the template needs more than the placeholder Cython source
